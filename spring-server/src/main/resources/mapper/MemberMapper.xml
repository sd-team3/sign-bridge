<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.soldesk.mapper.MemberMapper">
    <resultMap id="memberResultMap" type="com.soldesk.vo.MemberVO">
        <id column="member_id" property="memberId" />
        <result column="member_email" property="memberEmail" />
        <result column="member_password" property="memberPassword" />
        <result column="member_name" property="memberName" />
        <result column="member_profile_image" property="memberProfileImage" />
        <result column="role" property="role" />
        <result column="reg_date" property="regDate" />
        <result column="provider" property="provider" />
        <result column="provider_id" property="providerId" />
    </resultMap>
    <resultMap id="dailySignupResultMap" type="com.soldesk.vo.DailySignupVO">
        <result column="reg_day" property="day" />
        <result column="cnt" property="count" />
    </resultMap>


    <select id="findByEmail" parameterType="string" resultMap="memberResultMap">
        select member_id, member_password, member_email, member_name, member_profile_image, role, status, reg_date
        from member where member_email = #{memberEmail}
    </select>
    <insert id="insertMember" parameterType="com.soldesk.vo.MemberVO">
        insert into member (member_email, member_password, member_name, member_profile_image)
        values (#{memberEmail}, #{memberPassword}, #{memberName}, #{memberProfileImage})
    </insert>

    <select id="findByProviderAndProviderId" resultMap="memberResultMap">
        select member_id, member_email, member_password, member_name,
        member_profile_image, role, provider, provider_id
        from member
        where provider = #{provider} and provider_id = #{providerId}
    </select>
    <insert id="insertMemberOAuthMember" parameterType="com.soldesk.vo.MemberVO">
        insert into member (member_email, member_password, member_name, provider, provider_id)
        values(#{memberEmail}, #{memberPassword}, #{memberName}, #{provider}, #{providerId})
    </insert>

    <!-- 전체 회원 수 -->
    <select id="countAll" resultType="int">
        select count(*)
        from member
    </select>

    <!-- 오늘 추가된 회원 수 -->
    <select id="countTodayMember" resultType="int">
        SELECT COUNT(*) 
        FROM member 
        WHERE DATE(reg_date) = CURDATE();
    </select>

    <!-- 주간 일별 유저 추가 수 -->
    <select id="getWeeklySignupList" resultMap="dailySignupResultMap">
        SELECT DATE(reg_date) AS reg_day, COUNT(*) AS cnt
        FROM member
        WHERE reg_date >= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE(reg_date)
        ORDER BY reg_day
    </select>
    <!-- role에 따라 검색 기능 -->
    <select id="getAllMembers" resultMap="memberResultMap">
        SELECT
            m.member_id,
            m.member_name,
            m.member_email,
            m.role,
            m.status,
            m.reg_date,
            s.end_date
        FROM member m
        LEFT JOIN member_suspension s
            ON s.member_id = m.member_id
            AND (s.end_date IS NULL OR s.end_date &gt; NOW())
        <where>
            <if test="role != null and role != ''">
                AND m.role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND m.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (m.member_name LIKE CONCAT('%', #{keyword}, '%')
                    OR m.member_email LIKE CONCAT('%', #{keyword}, '%')
                    OR m.member_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        <choose>
            <when test="sort == 'oldest'">ORDER BY m.reg_date ASC</when>
            <when test="sort == 'name'">ORDER BY m.member_name ASC</when>
            <otherwise>ORDER BY m.reg_date DESC</otherwise>
        </choose>
        LIMIT #{start}, #{count}
    </select>

    
    <select id="getMemberCount" resultType="int">
        SELECT COUNT(*)
        FROM member m
        <where>
            <if test="role != null and role != ''">
                AND m.role = #{role}
            </if>
            <if test="status != null and status != ''">
                AND m.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (m.member_name LIKE CONCAT('%', #{keyword}, '%')
                    OR m.member_email LIKE CONCAT('%', #{keyword}, '%')
                    OR m.member_id LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
    </select>

    <select id="findById" resultMap="memberResultMap">
        select member_id, member_name, member_email, reg_date, provider, status, member_password
        from member
        where member_id = #{memberId}
    </select>

    <select id="getMemberByEmail" resultMap="memberResultMap">
        select member_id
        from member
        where member_email = #{email}
    </select>

    <!-- 로그인 전에 정지 기한 검사하고 풀어주기 -->
    <update id="updateStatus">
        update member
        set status = #{status}
        where member_id = #{memberId}   
    </update>

    <!-- 정지 추가하면 정지 때리기 -->
    <update id="suspendStatus">
        update member
        set status = 'SUSPEND'
        where member_id = #{memberId}
    </update>

    <!-- 회원만 삭제 -->
    <delete id="deleteMember">
        delete 
        from member
        where member_id = #{memberId}
    </delete>

    <update id="updateMemberName">
        update member
        set member_name = #{memberName}
        where member_id = #{memberId}
    </update>

    <update id="updatePassword">
        update member 
        set member_password = #{encodedPassword} 
        where member_id = #{memberId}
    </update>

    <!-- 끝말잇기 등에서 획득한 점수를 누적 -->
    <update id="addPoint">
        update member
        set point = point + #{delta}
        where member_id = #{memberId}
    </update>

</mapper>